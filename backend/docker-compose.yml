# backend/docker-compose.yml
services:
  backend:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env

  worker:
    build: .
    command: celery -A app.workers.celery_app worker --loglevel=info
    env_file:
      - .env